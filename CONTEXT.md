# 天气 App

面向 Android 零基础学习者的天气展示应用：用户查看某一**地点**的**当前天气**与（v2 起）**多日预报**，可搜索切换地点、收藏常用地点，或通过设备**定位**获取附近天气。

## Language

**地点**：
用户关注天气的地理实体，由经纬度唯一标识；展示名来自地理编码结果（如「北京」）。
_Avoid_: 城市（当泛指任意 geocode 结果时）、位置

**当前天气**：
某一时刻该地点的温度、湿度、风速、天气现象等实时快照。
_Avoid_: 实时天气、now

**多日预报**：
自今日起连续 7 天的逐日概要（最高/最低温、天气现象、可选降水量）；UI 为 Hero 下方的横向预报条；「今天」项为日聚合，与 Hero 实时**当前天气**并存展示。
_Avoid_: 未来天气、长期预报

**收藏地点**：
用户主动保存、可快速切换的地点列表条目；与「当前选中地点」独立。DataStore JSON 列表持久化，最多 10 条，按收藏时间降序；TopAppBar Star 切换当前地点的收藏状态，Bookmark 打开列表 Sheet。
_Avoid_: 收藏城市、书签

**当前选中地点**：
主界面正在展示天气的地点；切换搜索或点选收藏会改变它。TopAppBar 标题即其展示名；v2.1 起经 DataStore 持久化（name、经纬度、timezone、country）。
_Avoid_: 当前城市、active city

**默认地点**：
应用冷启动且尚无用户选择时使用的地点；v2.1 起持久化**上次选中地点**，无记录时回退北京。
_Avoid_: 初始城市、home city

**定位**：
通过设备 GPS（及权限）得到的坐标获取该处天气；展示名优先由 Android Geocoder 反查，失败时显示「当前位置」。TopAppBar MyLocation 触发；v2.3 引入。
_Avoid_: GPS 城市、自动定位

**天气现象**：
对用户可见的天气描述（如「晴」「小雨」），由 WMO 天气码在客户端映射为中文。
_Avoid_: 天气代码、weather code（对用户可见文案场景）

**数据来源标注**：
界面须展示的 Open-Meteo 出处与许可说明（CC BY 4.0）。
_Avoid_: 版权信息、footer
