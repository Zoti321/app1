# MVP 固定北京、不含搜索/GPS/缓存/多城市

首版目标是打通「网络 → 解析 → 状态 → UI」闭环，而非功能完备。预设北京消除 geocode 交互与权限复杂度；不含 Room/DataStore、城市搜索、GPS、多城市收藏与多日预报。v2 路线图单独决策上述扩展的边界与顺序。

**Considered options**: 最小可运行闭环（选用）、首版即含搜索或定位（拒绝，分散学习重点）
